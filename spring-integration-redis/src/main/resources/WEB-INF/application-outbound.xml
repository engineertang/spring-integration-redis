<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-redis="http://www.springframework.org/schema/integration/redis"
       xsi:schemaLocation="http://www.springframework.org/schema/integration/redis
  http://www.springframework.org/schema/integration/redis/spring-integration-redis.xsd
 http://www.springframework.org/schema/integration
 http://www.springframework.org/schema/integration/spring-integration.xsd
 http://www.springframework.org/schema/beans
 http://www.springframework.org/schema/beans/spring-beans.xsd">

    <int-redis:queue-outbound-channel-adapter
            id="event-outbound-channel-adapter"
            channel="eventChannelJson"
            auto-startup="true"
            serializer="serializer"
            connection-factory="redisConnectionFactory"
            queue="my-event-queue" />

    <int:gateway id="eventChannelGateway"
                 service-interface="com.springboot.intfc.RedisChannelGateway"
                 error-channel="errorChannel"
                 default-request-channel="eventChannel">
        <int:default-header name="topic" value="queue"/>
    </int:gateway>

    <!--When you use the <channel/> element without any sub-elements, it creates a DirectChannel instance (a SubscribableChannel)-->
    <int:channel id="eventChannelJson"/>
    <int:channel id="eventChannel"/>

    <bean id="serializer" class="org.springframework.data.redis.serializer.StringRedisSerializer"/>

    <int:object-to-json-transformer input-channel="eventChannel" output-channel="eventChannelJson"/>

    <int-redis:queue-inbound-channel-adapter id="event-inbound-channel-adapter"
                                             channel="inboundEventChannelJson"
                                             queue="my-event-queue"
                                             auto-startup="true"
                                             serializer="serializer"
                                             connection-factory="redisConnectionFactory"/>

    <int:channel id="inboundEventChannelJson"/>

    <!--QueueChannel Configuration-->
    <int:channel id="inboundEventChannel">

    </int:channel>

    <int:json-to-object-transformer input-channel="inboundEventChannelJson"
                                    output-channel="inboundEventChannel"
                                    type="com.springboot.model.Student"/>

    <int:service-activator input-channel="inboundEventChannel"
                           ref="redisEventProcessingService"
                           method="processStudent">
       <!-- <int:poller fixed-delay="10" time-unit="SECONDS" max-messages-per-poll="500"/>-->
    </int:service-activator>

    <bean name="redisEventProcessingService" class="com.springboot.listener.RedisEventProcessingService" />
</beans>